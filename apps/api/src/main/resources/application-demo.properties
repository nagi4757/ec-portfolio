# Database
spring.datasource.url=jdbc:mariadb://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}?sslMode=verify-full&serverSslCert=/etc/ssl/certs/aws-rds-ap-northeast-1-bundle.pem&fallbackToSystemTrustStore=false
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Redis
# Plaintext is limited to the same-host private Docker network used by the AWS demo runtime.
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.ssl.enabled=false

# Actuator
management.endpoint.health.group.readiness.include=readinessState,db,redis

# Auth
app.auth.jwt.secret=${APP_AUTH_JWT_SECRET}

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}

# OpenAPI
app.openapi.enabled=${APP_OPENAPI_ENABLED:false}

# Logging
logging.structured.format.console=ecs
logging.structured.ecs.service.environment=demo
