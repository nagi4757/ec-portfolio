# Actuator
management.endpoint.health.group.readiness.include=readinessState,db,redis

# Auth
app.auth.jwt.secret=integration-test-only-dummy-jwt-secret-at-least-32-bytes-long

# CORS
app.cors.allowed-origins=https://integration.test
